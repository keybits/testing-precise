machine:
  python:
    version: 2.7.10

dependencies:
  cache_directories:
    - ~/downloads/


test:
  pre:
    - mkdir -p ~/downloads
  post:
    - touch $CIRCLE_ARTIFACTS/file.txt
    - touch ~/downloads/tom.txt

deployment:
  release:
    tag: /.*/
    owner: keybits
    commands:
      - "true"